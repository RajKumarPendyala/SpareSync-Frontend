import { Text, View } from 'react-native';
import React from 'react';

const AdminTabNav = ({navigation}) => {
    return (
      <View>
        <Text>AdminTabNav</Text>
      </View>
    );
};

export default AdminTabNav;
